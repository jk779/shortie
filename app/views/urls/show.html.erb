<p style="color: green"><%= notice %></p>

<%= "ID: #{@url.id}: #{@url.original_url} → #{@url.short_url}" %>

<div>
  <%= link_to "Edit this url", edit_url_path(@url) %> |
  <%= link_to "Back to urls", urls_path %>

  <%= button_to "Destroy this url", @url, method: :delete %>
</div>
